# RESTART PROMPT — Gateway Solid for Kv1.1 + CustomGPT Integration

**Date:** 2026-01-09
**Artifact Type:** restart
**Purpose:** Session continuation prompt for Gateway build session

---

## Overall Goal

Get Gateway solidly working for all Kv1.1 capabilities, then bring up Qwrk's CustomGPT front end and confirm it interacts successfully with Gateway.

---

## Non-Negotiable Scope for Next Session (Phase A)

**Artifact types:** project, journal, snapshot, restart (KGB kernel types)

**Capabilities:**
- save (single action for both INSERT and UPDATE)
- query (by id + minimal field selectors)
- list (MVP)
- hydrate (spine + extension)
- update where allowed

**Mutability enforcement:**
- **journal:** UPDATE BLOCKED (insert-only)
- **snapshot:** UPDATE BLOCKED (immutable)
- **restart:** UPDATE BLOCKED (immutable)
- **project:** UPDATE ALLOWED (limited fields only)

---

## Contract Invariants

1. **Use artifact.save (single save action)** - Do NOT introduce artifact.create
2. **Spine-first:** qxb_artifact is fetched/validated first; stored artifact_type is truth
3. **Response shaping must not depend on post-DB nodes** that drop request context
4. **After each meaningful change:** run a small regression set to prevent query/list regressions

---

## CURRENT BLOCKER (Must Be Fixed First)

**Problem:**
- artifact.save INSERT succeeds (DB returns artifact_id)
- BUT final Gateway response returns:
  - `workspace_id = null`
  - `artifact_type = null`
  - `extension.payload = {}` (should echo what was saved for restart/snapshot)

**Diagnosis:**
Response is being built from a later node output that lost key fields.

---

## Step 1 — Fix artifact.save INSERT Response Shaping (First Work Item)

**Action:**
- Freeze required request intent fields early (before any DB nodes):
  - `req_artifact_type`
  - `req_workspace_id`
  - `req_owner_user_id` (if used)
  - `req_extension_payload` (for restart/snapshot)
- Build final response ONLY from frozen req_* fields + inserted artifact_id + operation/timestamp

**Minimum response contract for INSERT:**
```json
{
  "ok": true,
  "gw_action": "artifact.save",
  "artifact_id": "<uuid>",
  "artifact_type": "<type>",
  "workspace_id": "<uuid>",
  "operation": "INSERT",
  "timestamp": "<iso8601>",
  "extension": {
    "payload": {} // for restart/snapshot
  }
}
```

**Regression gate after Step 1:**
- Re-run artifact.query KGB tests for: project, journal, snapshot, restart
- If artifact.list already exists, run 1 basic list test
- Run 1 restart save INSERT test and confirm response contract fields are non-null

---

## Step 2 — Implement artifact.list (MVP)

**Scope:**
- Type-scoped list only (cross-type list deferred)

**Minimal selector:**
- `artifact_type` (required)
- `parent_artifact_id` (optional)
- `limit` (default 20; max 100)
- optional offset/cursor if already supported

**Return:**
Fully hydrated artifacts: spine + extension

**Regression gate after Step 2:**
- Query KGB tests still pass
- List returns correct rows and hydration for at least 2 types

---

## Step 3 — Implement artifact.update (Only Where Allowed)

**Enforcement:**
- Enforce immutability rules listed above
- Allow project updates only (limit fields; no client-supplied version)

---

## Step 4 — CustomGPT Integration (Only After Steps 1–3 Pass)

**Action:**
- Align GPT Action schema to Gateway contract (artifact.save, artifact.query, artifact.list, artifact.update)

**End-to-end tests via CustomGPT:**
1. Create journal (save)
2. Query journal
3. List journals
4. Attempt journal update (must be blocked)
5. Create project
6. Update project

---

## Optional Phase B (ONLY If Phase A Is Complete and Time Remains)

Expand artifact_type support beyond KGB types ONLY if:
- Schema tables exist and are confirmed
- Allow-lists/contracts updated
- Tests added

**No guessing. If schema is not confirmed, stop.**

---

## Known-Good State (KGB)

From previous session (2025-12-31):

**Working:**
- artifact.query works end-to-end for all 4 KGB types
- Spine-first architecture validated
- Type-specific branching working
- Response merging working (strips redundant extension.artifact_type)

**Validated test IDs:**
- journal: `db428a32-1afa-4e6b-a649-347b0bffd46c`
- project: `668bd18f-4424-41e6-b2f9-393ecd2ec534`
- snapshot: `610e16d1-c5bb-468c-bd35-57eadf9f2e38`
- restart: `ac1d6294-2bd7-4a9d-823e-827562b56e26`

---

## Hard Rules (n8n Workflow Guidelines)

1. **Expression syntax:** DO NOT type a leading `=` in n8n expressions; n8n adds it automatically
2. **Supabase nodes are dumb column writers:** Flatten payloads before DB nodes; don't auto-map wrapped payloads
3. **Node naming discipline:** Use `Qxb`-prefixed names consistently
4. **Switch comparison safety:** Guard against hidden whitespace/newlines using `.trim()`
5. **No guessing:** Do not guess schemas, enums, endpoints, or commands. Stop and ask for canonical source if unclear.

---

## Operating Style

- Provide one or two steps at a time, then wait for confirmation/results
- Use "we/our issue" phrasing during troubleshooting
- When referencing node names/identifiers, place them in markdown code blocks for copy/paste

---

## Session Start Procedure

1. User will upload the current workflow export (`NQxb_Gateway_v1.json`)
2. Your first action: Locate the broken Save response shaping code
3. Design the fix
4. Proceed with Step 1 implementation

---

**Status:** Ready for execution
**Priority:** Critical (gates CustomGPT launch)
**Owner:** Master Joel + Claude Code

---
